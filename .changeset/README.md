# Changesets

Toda alteração em `packages/*` que afete consumidores deve vir acompanhada de um changeset (`pnpm changeset`).
Publicação em registry está fora do escopo da Sprint 0 — o versionamento já nasce disciplinado.
