---
title: DropdownMenu
description: "Menu de ações ancorado a um gatilho."
---

É para comandos, não para entrada de dados: para escolher um valor de formulário use `Select`. Suporta submenus via `DropdownMenuSub`.

Disponível em `@design-systems-orion/ui`, na categoria **Navegação**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
pnpm add @design-systems-orion/ui
```

## Import

```tsx
import {
	DropdownMenu,
	DropdownMenuTrigger,
	DropdownMenuContent,
	DropdownMenuItem,
	DropdownMenuCheckboxItem,
	DropdownMenuLabel,
	DropdownMenuSeparator,
	DropdownMenuShortcut,
	DropdownMenuGroup,
	DropdownMenuPortal,
	DropdownMenuSub,
	DropdownMenuSubContent,
	DropdownMenuSubTrigger,
} from "@design-systems-orion/ui/dropdown-menu";
```

O import por subpath traz só `dropdown-menu`. Importar de `@design-systems-orion/ui` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="ui/dropdown-menu" story="Default" />

## API Reference

### DropdownMenuContent

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `sideOffset` | `number` | `4` |
| `align` | `"center" \| "start" \| "end"` | `start` |

### DropdownMenuItem

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `inset` | `boolean` | — |

### DropdownMenuLabel

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `inset` | `boolean` | — |

### DropdownMenuSubTrigger

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `inset` | `boolean` | — |

Além destas, `DropdownMenu` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Com checkbox esubmenu

<ComponentPreview name="ui/dropdown-menu" story="ComCheckboxESubmenu" />

## Storybook

Todas as variações de `DropdownMenu`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

