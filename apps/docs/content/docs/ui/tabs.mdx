---
title: Tabs
description: Componente da biblioteca Base UI.
---

import { Tabs } from "@portais-orion/ui";

# Tabs

Um componente da biblioteca `@portais-orion/ui` agrupado em **Navigation**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { Tabs } from "@portais-orion/ui";

export default function Example() {
  return (
    <Tabs {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### Line
Consulte o Storybook para a variação `Line`.

### Vertical
Consulte o Storybook para a variação `Vertical`.

### VerticalLine
Consulte o Storybook para a variação `VerticalLine`.
