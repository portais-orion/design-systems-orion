---
title: Combobox
description: Componente da biblioteca Base UI.
---

import { Combobox } from "@portais-orion/ui";

# Combobox

Um componente da biblioteca `@portais-orion/ui` agrupado em **Forms**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { Combobox } from "@portais-orion/ui";

export default function Example() {
  return (
    <Combobox {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### WithSearch
Consulte o Storybook para a variação `WithSearch`.

### WithGroups
Consulte o Storybook para a variação `WithGroups`.

### Disabled
Consulte o Storybook para a variação `Disabled`.

### Clearable
Consulte o Storybook para a variação `Clearable`.

### WithDescriptions
Consulte o Storybook para a variação `WithDescriptions`.

### EmptySearch
Consulte o Storybook para a variação `EmptySearch`.

### LongList
Consulte o Storybook para a variação `LongList`.

### KeyboardInteraction
Consulte o Storybook para a variação `KeyboardInteraction`.

### ComLabelExterno
Consulte o Storybook para a variação `ComLabelExterno`.
