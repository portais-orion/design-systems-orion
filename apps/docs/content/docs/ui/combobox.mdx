---
title: Combobox
description: "Seleção de uma opção com busca por digitação, para listas longas demais para um `Select`."
---

O próprio campo é o input de busca, e `options` aceita itens planos ou agrupados. Para seleção múltipla, use `MultiSelect`.

Disponível em `@design-systems-orion/ui`, na categoria **Formulários**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
pnpm add @design-systems-orion/ui
```

## Import

```tsx
import { Combobox } from "@design-systems-orion/ui/combobox";
```

O import por subpath traz só `combobox`. Importar de `@design-systems-orion/ui` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="ui/combobox" story="Default" />

## API Reference

| Prop | Tipo | Padrão | Descrição |
| --- | --- | --- | --- |
| `value` | `string` | `null` | — |
| `onValueChange`\* | `(value: string) => void` | — | — |
| `options`\* | `AdvancedOptions` | — | — |
| `placeholder` | `string` | `Selecione...` | — |
| `searchPlaceholder` | `string` | — | Mantido por compatibilidade de API: no padrão Base UI o próprio campo é a busca. |
| `emptyMessage` | `string` | `Nenhuma opção encontrada` | — |
| `disabled` | `boolean` | `false` | — |
| `clearable` | `boolean` | `false` | — |
| `className` | `string` | — | — |
| `contentClassName` | `string` | — | — |
| `id` | `string` | — | id repassado ao input (integração com Label). |
| `aria-label` | `string` | — | — |

\* Prop obrigatória.

Além destas, `Combobox` aceita as props do elemento/primitivo que envolve.

## Exemplos

### With search

<ComponentPreview name="ui/combobox" story="WithSearch" />

### With groups

<ComponentPreview name="ui/combobox" story="WithGroups" />

### Disabled

<ComponentPreview name="ui/combobox" story="Disabled" />

### Clearable

<ComponentPreview name="ui/combobox" story="Clearable" />

### With descriptions

<ComponentPreview name="ui/combobox" story="WithDescriptions" />

### Empty search

<ComponentPreview name="ui/combobox" story="EmptySearch" />

### Long list

<ComponentPreview name="ui/combobox" story="LongList" />

### Keyboard interaction

<ComponentPreview name="ui/combobox" story="KeyboardInteraction" />

### Com label externo

<ComponentPreview name="ui/combobox" story="ComLabelExterno" />

## Storybook

Todas as variações de `Combobox`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

