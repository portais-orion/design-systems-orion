---
title: Progress
description: "Barra de progresso de uma tarefa."
---

`value` ausente ou `null` significa progresso indeterminado e renderiza uma barra animada.

Disponível em `@portais-orion/ui`, na categoria **Feedback**.

## Instalação

```bash
# O ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<ComponentPreview name="ui/progress" story="Default" />

## API Reference

| Prop | Tipo | Padrão | Descrição |
| --- | --- | --- | --- |
| `max` | `number` | `100` | The maximum value. Valor que representa 100%. |
| `value` | `number` | `null` | Progresso atual. `null` (padrão) rende a barra indeterminada. |
| `indicatorClassName` | `string` | — | Classes aplicadas à barra preenchida, não ao trilho. |

Além destas, `Progress` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Different values

<ComponentPreview name="ui/progress" story="DifferentValues" />

### Indeterminate

<ComponentPreview name="ui/progress" story="Indeterminate" />

### With label

<ComponentPreview name="ui/progress" story="WithLabel" />

## Storybook

Todas as variações de `Progress`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

