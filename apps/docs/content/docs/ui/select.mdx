---
title: Select
description: "Seleção de uma opção entre poucas, a partir de uma lista fechada."
---

Quando a lista é longa e pede busca, use `Combobox`; para várias opções ao mesmo tempo, `MultiSelect`.

Disponível em `@portais-orion/ui`, na categoria **Formulários**.

## Instalação

```bash
# O ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<ComponentPreview name="ui/select" story="Default" />

## API Reference

### SelectValue

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `items` | `{ value: string; label: string; }[]` | — |

### SelectContent

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `position` | `"popper" \| "item-aligned"` | `popper` |

Além destas, `Select` aceita as props do elemento/primitivo que envolve.

## Exemplos

### With items array

<ComponentPreview name="ui/select" story="WithItemsArray" />

## Storybook

Todas as variações de `Select`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

