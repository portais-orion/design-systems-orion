---
title: Button
description: "Aciona uma ação."
---

Para navegar até outra página, passe `render` com um link — assim o elemento continua sendo um `<a>` para o leitor de tela e o teclado. Ícones passados como filhos são dimensionados automaticamente.

Disponível em `@design-systems-orion/ui`, na categoria **Ações**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
pnpm add @design-systems-orion/ui
```

## Import

```tsx
import { Button } from "@design-systems-orion/ui/button";
```

O import por subpath traz só `button`. Importar de `@design-systems-orion/ui` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="ui/button" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `variant` | `"link" \| "default" \| "outline" \| "secondary" \| "ghost" \| "destructive"` | `default` |
| `size` | `"default" \| "xs" \| "sm" \| "lg" \| "icon" \| "icon-xs" \| "icon-sm" \| "icon-lg"` | `default` |

Além destas, `Button` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Outline

<ComponentPreview name="ui/button" story="Outline" />

### Secondary

<ComponentPreview name="ui/button" story="Secondary" />

### Ghost

<ComponentPreview name="ui/button" story="Ghost" />

### Destructive

<ComponentPreview name="ui/button" story="Destructive" />

### Link

<ComponentPreview name="ui/button" story="Link" />

### Disabled

<ComponentPreview name="ui/button" story="Disabled" />

### With icon

<ComponentPreview name="ui/button" story="WithIcon" />

### All sizes

<ComponentPreview name="ui/button" story="AllSizes" />

## Storybook

Todas as variações de `Button`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

