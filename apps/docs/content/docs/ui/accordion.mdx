---
title: Accordion
description: Componente da biblioteca Base UI.
---

import { Accordion } from "@portais-orion/ui";

# Accordion

Um componente da biblioteca `@portais-orion/ui` agrupado em **Data Display**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { Accordion } from "@portais-orion/ui";

export default function Example() {
  return (
    <Accordion {/* props */} />
  )
}
```

## Exemplos

### Single
Consulte o Storybook para a variação `Single`.

### Multiple
Consulte o Storybook para a variação `Multiple`.

### WithLongContent
Consulte o Storybook para a variação `WithLongContent`.

### DisabledItem
Consulte o Storybook para a variação `DisabledItem`.
