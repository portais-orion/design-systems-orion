---
title: Checkbox
description: "Caixa de seleção para escolhas independentes (marcar/desmarcar)."
---

Controle com `checked` e `onCheckedChange`; associe um `Label` via `id`/`htmlFor`.

Disponível em `@portais-orion/ui`, na categoria **Formulários**.

## Instalação

```bash
# O ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<ComponentPreview name="ui/checkbox" story="Default" />

## API Reference

`Checkbox` não declara props próprias: repassa as props do primitivo
de Base UI que envolve, mais `className`. Consulte a [documentação do Base UI](https://base-ui.com/react/overview/quick-start)
para a lista completa.

## Exemplos

### Checked

<ComponentPreview name="ui/checkbox" story="Checked" />

### Disabled

<ComponentPreview name="ui/checkbox" story="Disabled" />

## Storybook

Todas as variações de `Checkbox`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

