---
title: Card
description: "Superfície elevada que agrupa conteúdo relacionado."
---

Componha com `CardHeader`, `CardContent` e `CardFooter`; o padding é responsabilidade dessas partes, não da raiz.

Disponível em `@portais-orion/ui`, na categoria **Exibição de dados**.

## Instalação

```bash
# O ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<ComponentPreview name="ui/card" story="Default" />

## API Reference

`Card` não declara props próprias: repassa as props do primitivo
de Base UI que envolve, mais `className`. Consulte a [documentação do Base UI](https://base-ui.com/react/overview/quick-start)
para a lista completa.

## Exemplos

### Simple

<ComponentPreview name="ui/card" story="Simple" />

## Storybook

Todas as variações de `Card`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

