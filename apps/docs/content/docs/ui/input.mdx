---
title: Input
description: "Campo de texto de uma linha."
---

Aceita qualquer `type` de `<input>`; associe um `Label` via `id`/`htmlFor` e use `FormField` (blocks) quando precisar de rótulo, ajuda e mensagem de erro juntos.

Disponível em `@portais-orion/ui`, na categoria **Formulários**.

## Instalação

```bash
# O ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<ComponentPreview name="ui/input" story="Default" />

## API Reference

`Input` não declara props próprias: repassa as props do primitivo
de Base UI que envolve, mais `className`. Consulte a [documentação do Base UI](https://base-ui.com/react/overview/quick-start)
para a lista completa.

## Exemplos

### Password

<ComponentPreview name="ui/input" story="Password" />

### Disabled

<ComponentPreview name="ui/input" story="Disabled" />

### With value

<ComponentPreview name="ui/input" story="WithValue" />

## Storybook

Todas as variações de `Input`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

