---
title: Popover
description: Componente da biblioteca Base UI.
---

import { Popover } from "@portais-orion/ui";

# Popover

Um componente da biblioteca `@portais-orion/ui` agrupado em **Data Display**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { Popover } from "@portais-orion/ui";

export default function Example() {
  return (
    <Popover {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### WithForm
Consulte o Storybook para a variação `WithForm`.

### WithCustomWidth
Consulte o Storybook para a variação `WithCustomWidth`.

### SupertransBrand
Consulte o Storybook para a variação `SupertransBrand`.

### AuroraBrand
Consulte o Storybook para a variação `AuroraBrand`.
