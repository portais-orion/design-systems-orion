---
title: Sheet
description: Componente da biblioteca Base UI.
---

import { Sheet } from "@portais-orion/ui";

# Sheet

Um componente da biblioteca `@portais-orion/ui` agrupado em **Feedback**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/ui
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { Sheet } from "@portais-orion/ui";

export default function Example() {
  return (
    <Sheet {/* props */} />
  )
}
```

## Exemplos

### Right
Consulte o Storybook para a variação `Right`.

### Left
Consulte o Storybook para a variação `Left`.

### Bottom
Consulte o Storybook para a variação `Bottom`.

### WithFormContent
Consulte o Storybook para a variação `WithFormContent`.

### ScrollableContent
Consulte o Storybook para a variação `ScrollableContent`.
