---
title: FormField
description: Componente da biblioteca Blocks.
---

import { FormField } from "@portais-orion/blocks";

# FormField

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Forms**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { FormField } from "@portais-orion/blocks";

export default function Example() {
  return (
    <FormField {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### WithDescription
Consulte o Storybook para a variação `WithDescription`.

### WithError
Consulte o Storybook para a variação `WithError`.

### Required
Consulte o Storybook para a variação `Required`.

### WithTextarea
Consulte o Storybook para a variação `WithTextarea`.

### WithCombobox
Consulte o Storybook para a variação `WithCombobox`.

### WithMultiSelect
Consulte o Storybook para a variação `WithMultiSelect`.

### WithCheckbox
Consulte o Storybook para a variação `WithCheckbox`.
