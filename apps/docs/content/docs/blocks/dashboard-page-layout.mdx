---
title: DashboardPageLayout
description: "Layout de dashboard: faixa de indicadores no topo, conteúdo principal e `aside` opcional em coluna lateral."
---

Disponível em `@design-systems-orion/blocks`, na categoria **Layout**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @design-systems-orion/ui junto.
pnpm add @design-systems-orion/blocks
```

## Import

```tsx
import { DashboardPageLayout } from "@design-systems-orion/blocks/dashboard-page-layout";
```

O import por subpath traz só `dashboard-page-layout`. Importar de `@design-systems-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/dashboard-page-layout" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `header` | `any` | — |
| `stats` | `any` | — |
| `content`\* | `any` | — |
| `aside` | `any` | — |
| `className` | `string` | — |

\* Prop obrigatória.

Além destas, `DashboardPageLayout` aceita as props do elemento/primitivo que envolve.

## Exemplos

### With stats

<ComponentPreview name="blocks/dashboard-page-layout" story="WithStats" />

### With aside

<ComponentPreview name="blocks/dashboard-page-layout" story="WithAside" />

### With cards grid

<ComponentPreview name="blocks/dashboard-page-layout" story="WithCardsGrid" />

## Storybook

Todas as variações de `DashboardPageLayout`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

