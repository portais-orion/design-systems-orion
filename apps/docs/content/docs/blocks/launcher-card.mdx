---
title: LauncherCard
description: "Card de entrada para um módulo, usado nas grades de home."
---

Não conhece rota: passe `onClick` (e navegue com o router do consumidor) ou envolva o card com o seu próprio link. Com `onClick`, renderiza um `<button>`.

Disponível em `@portais-orion/blocks`, na categoria **Exibição de dados**.

## Instalação

```bash
# O ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<ComponentPreview name="blocks/launcher-card" story="Default" />

## API Reference

| Prop | Tipo | Padrão | Descrição |
| --- | --- | --- | --- |
| `title`\* | `string` | — | — |
| `description` | `string` | — | — |
| `icon` | `ComponentType<{ className?: string; }>` | — | — |
| `cta` | `any` | — | CTA textual no rodapé (ex.: "Abrir →"). |
| `onClick` | `() => void` | — | — |
| `className` | `string` | — | — |

\* Prop obrigatória.

Além destas, `LauncherCard` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Clicavel

<ComponentPreview name="blocks/launcher-card" story="Clicavel" />

### Grade

<ComponentPreview name="blocks/launcher-card" story="Grade" />

## Storybook

Todas as variações de `LauncherCard`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

