---
title: AttachmentList
description: "Lista de anexos: ícone tonalizado, nome, data e extensão, com seleção (ex.: abrir preview) e remoção opcionais."
---

Não sabe renderizar o preview do arquivo em si — isso fica com o consumidor.

Disponível em `@portais-orion/blocks`, na categoria **Exibição de dados**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @portais-orion/ui junto.
pnpm add @portais-orion/blocks
```

## Import

```tsx
import { AttachmentList } from "@portais-orion/blocks/attachment-list";
```

O import por subpath traz só `attachment-list`. Importar de `@portais-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/attachment-list" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `items`\* | `AttachmentItem[]` | — |
| `onSelect` | `(item: AttachmentItem) => void` | — |
| `onRemove` | `(item: AttachmentItem) => void` | — |
| `removeLabel` | `string` | `Remover` |
| `emptyMessage` | `string` | `Nenhum anexo.` |
| `className` | `string` | — |

\* Prop obrigatória.

Além destas, `AttachmentList` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Vazio

<ComponentPreview name="blocks/attachment-list" story="Vazio" />

## Storybook

Todas as variações de `AttachmentList`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

