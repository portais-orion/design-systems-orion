---
title: AppShell
description: Componente da biblioteca Blocks.
---

import { AppShell } from "@portais-orion/blocks";

# AppShell

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Layout**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { AppShell } from "@portais-orion/blocks";

export default function Example() {
  return (
    <AppShell {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### WithBreadcrumbs
Consulte o Storybook para a variação `WithBreadcrumbs`.

### Collapsed
Consulte o Storybook para a variação `Collapsed`.

### WithFilteredNavigation
Consulte o Storybook para a variação `WithFilteredNavigation`.

### ControlledCollapsed
Consulte o Storybook para a variação `ControlledCollapsed`.

### MobileNavigation
Consulte o Storybook para a variação `MobileNavigation`.

### AppShellWithListPage
Consulte o Storybook para a variação `AppShellWithListPage`.

### AppShellWithFormPage
Consulte o Storybook para a variação `AppShellWithFormPage`.

### AppShellWithDetailPage
Consulte o Storybook para a variação `AppShellWithDetailPage`.

### AppShellWithDashboardPage
Consulte o Storybook para a variação `AppShellWithDashboardPage`.
