---
title: AppShell
description: "Chrome do portal: sidebar fixa, barra de breadcrumbs e a área de conteúdo."
---

Não tem topbar, por decisão do padrão do grupo. Abaixo de `md` a sidebar vira um drawer com a mesma navegação. Não conhece rota, sessão nem permissão — passe a navegação em `sidebar` (ou monte a sua com `renderSidebar`).

Disponível em `@design-systems-orion/blocks`, na categoria **Layout**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @design-systems-orion/ui junto.
pnpm add @design-systems-orion/blocks
```

## Import

```tsx
import { AppShell } from "@design-systems-orion/blocks/app-shell";
```

O import por subpath traz só `app-shell`. Importar de `@design-systems-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/app-shell" story="Default" />

## API Reference

| Prop | Tipo | Padrão | Descrição |
| --- | --- | --- | --- |
| `sidebar` | `AppShellSidebar` | — | Navegação do portal — o caminho normal: o shell monta a `Sidebar`. |
| `renderSidebar` | `(props: AppShellSidebarSlotProps) => any` | — | Escape hatch para quem precisa de outra sidebar. Recebe o estado de colapso já resolvido pelo shell. Use `sidebar` quando puder. |
| `breadcrumbs` | `BreadcrumbItem[]` | — | — |
| `renderLink` | `RenderLink` | — | Injetor de links usado pelos breadcrumbs. |
| `collapsed` | `boolean` | — | Colapso controlado. Sem isso, o shell guarda o estado. |
| `defaultCollapsed` | `boolean` | `false` | — |
| `onCollapsedChange` | `(collapsed: boolean) => void` | — | — |
| `className` | `string` | — | — |
| `contentClassName` | `string` | — | — |

Além destas, `AppShell` aceita as props do elemento/primitivo que envolve.

## Exemplos

### With breadcrumbs

<ComponentPreview name="blocks/app-shell" story="WithBreadcrumbs" />

### Collapsed

<ComponentPreview name="blocks/app-shell" story="Collapsed" />

### With filtered navigation

<ComponentPreview name="blocks/app-shell" story="WithFilteredNavigation" />

### Controlled collapsed

<ComponentPreview name="blocks/app-shell" story="ControlledCollapsed" />

### Custom sidebar

<ComponentPreview name="blocks/app-shell" story="CustomSidebar" />

### Mobile navigation

<ComponentPreview name="blocks/app-shell" story="MobileNavigation" />

### App shell with list page

<ComponentPreview name="blocks/app-shell" story="AppShellWithListPage" />

### App shell with form page

<ComponentPreview name="blocks/app-shell" story="AppShellWithFormPage" />

### App shell with detail page

<ComponentPreview name="blocks/app-shell" story="AppShellWithDetailPage" />

### App shell with dashboard page

<ComponentPreview name="blocks/app-shell" story="AppShellWithDashboardPage" />

## Storybook

Todas as variações de `AppShell`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

