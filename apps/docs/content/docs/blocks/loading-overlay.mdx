---
title: LoadingOverlay
description: Componente da biblioteca Blocks.
---

import { LoadingOverlay } from "@portais-orion/blocks";

# LoadingOverlay

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Feedback**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { LoadingOverlay } from "@portais-orion/blocks";

export default function Example() {
  return (
    <LoadingOverlay {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### WithCard
Consulte o Storybook para a variação `WithCard`.

### WithTable
Consulte o Storybook para a variação `WithTable`.

### WithoutLabel
Consulte o Storybook para a variação `WithoutLabel`.
