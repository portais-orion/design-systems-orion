---
title: StatusCards
description: "Componente de exibição de dados da biblioteca Blocks."
---

Disponível em `@design-systems-orion/blocks`, na categoria **Exibição de dados**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @design-systems-orion/ui junto.
pnpm add @design-systems-orion/blocks
```

## Import

```tsx
import { StatusCards } from "@design-systems-orion/blocks/status-cards";
```

O import por subpath traz só `status-cards`. Importar de `@design-systems-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/status-cards" story="Default" />

## API Reference

`StatusCards` não declara props próprias: repassa as props do primitivo
de Base UI que envolve, mais `className`. Consulte a [documentação do Base UI](https://base-ui.com/react/overview/quick-start)
para a lista completa.

## Exemplos

### Sem icones

<ComponentPreview name="blocks/status-cards" story="SemIcones" />

### Como filtro clicavel

<ComponentPreview name="blocks/status-cards" story="ComoFiltroClicavel" />

## Storybook

Todas as variações de `StatusCards`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

