---
title: CodeBadge
description: "Exibe um código ou identificador de registro em fonte monoespaçada, para que sequências como `PRC-001` fiquem alinhadas e legíveis numa coluna."
---

Disponível em `@design-systems-orion/blocks`, na categoria **Utilitários**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @design-systems-orion/ui junto.
pnpm add @design-systems-orion/blocks
```

## Import

```tsx
import { CodeBadge } from "@design-systems-orion/blocks/code-badge";
```

O import por subpath traz só `code-badge`. Importar de `@design-systems-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/code-badge" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `className` | `string` | — |

Além destas, `CodeBadge` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Em texto

<ComponentPreview name="blocks/code-badge" story="EmTexto" />

## Storybook

Todas as variações de `CodeBadge`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

