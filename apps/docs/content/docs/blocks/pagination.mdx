---
title: Pagination
description: Componente da biblioteca Blocks.
---

import { Pagination } from "@portais-orion/blocks";

# Pagination

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Tabelas**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { Pagination } from "@portais-orion/blocks";

export default function Example() {
  return (
    <Pagination {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### SemTrocaDeLimite
Consulte o Storybook para a variação `SemTrocaDeLimite`.

### PaginaUnica
Consulte o Storybook para a variação `PaginaUnica`.

### TotalZeroNaoRenderiza
Consulte o Storybook para a variação `TotalZeroNaoRenderiza`.
