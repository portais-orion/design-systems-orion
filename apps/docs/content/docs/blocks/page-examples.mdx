---
title: PageExamples
description: Componente da biblioteca Blocks.
---

import { PageExamples } from "@portais-orion/blocks";

# PageExamples

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Layout**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { PageExamples } from "@portais-orion/blocks";

export default function Example() {
  return (
    <PageExamples {/* props */} />
  )
}
```

## Exemplos

### ListPageExample
Consulte o Storybook para a variação `ListPageExample`.

### FormPageExample
Consulte o Storybook para a variação `FormPageExample`.

### DetailPageExample
Consulte o Storybook para a variação `DetailPageExample`.

### DashboardPageExample
Consulte o Storybook para a variação `DashboardPageExample`.
