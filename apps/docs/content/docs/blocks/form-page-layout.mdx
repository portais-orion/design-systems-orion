---
title: FormPageLayout
description: "Layout de página de criar ou editar registro."
---

Sem `aside`, contém o formulário numa coluna estreita para não esticar demais as linhas; com `aside`, abre espaço lateral. Não cria o elemento `<form>` nem conhece biblioteca de formulário — isso fica com o consumidor.

Disponível em `@design-systems-orion/blocks`, na categoria **Layout**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @design-systems-orion/ui junto.
pnpm add @design-systems-orion/blocks
```

## Import

```tsx
import { FormPageLayout } from "@design-systems-orion/blocks/form-page-layout";
```

O import por subpath traz só `form-page-layout`. Importar de `@design-systems-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/form-page-layout" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `header` | `any` | — |
| `form`\* | `any` | — |
| `aside` | `any` | — |
| `footer` | `any` | — |
| `className` | `string` | — |

\* Prop obrigatória.

Além destas, `FormPageLayout` aceita as props do elemento/primitivo que envolve.

## Exemplos

### With aside

<ComponentPreview name="blocks/form-page-layout" story="WithAside" />

### Multiple sections

<ComponentPreview name="blocks/form-page-layout" story="MultipleSections" />

### With sticky actions

<ComponentPreview name="blocks/form-page-layout" story="WithStickyActions" />

## Storybook

Todas as variações de `FormPageLayout`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

