---
title: FormSection
description: "Agrupa campos relacionados sob um título, separando blocos de um formulário longo."
---

Renderiza um `<section>` com `<h3>`; use `FieldGroup` dentro para distribuir os campos em colunas.

Disponível em `@portais-orion/blocks`, na categoria **Formulários**.

## Instalação

```bash
# O ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<ComponentPreview name="blocks/form-section" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `title` | `any` | — |
| `description` | `any` | — |
| `actions` | `any` | — |
| `className` | `string` | — |
| `contentClassName` | `string` | — |

Além destas, `FormSection` aceita as props do elemento/primitivo que envolve.

## Exemplos

### With description

<ComponentPreview name="blocks/form-section" story="WithDescription" />

### With actions

<ComponentPreview name="blocks/form-section" story="WithActions" />

### Multiple sections

<ComponentPreview name="blocks/form-section" story="MultipleSections" />

## Storybook

Todas as variações de `FormSection`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

