---
title: FormSection
description: Componente da biblioteca Blocks.
---

import { FormSection } from "@portais-orion/blocks";

# FormSection

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Forms**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { FormSection } from "@portais-orion/blocks";

export default function Example() {
  return (
    <FormSection {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### WithDescription
Consulte o Storybook para a variação `WithDescription`.

### WithActions
Consulte o Storybook para a variação `WithActions`.

### MultipleSections
Consulte o Storybook para a variação `MultipleSections`.
