---
title: FormMessage
description: "Mensagem de apoio ou validação sob um campo."
---

Com `tone="error"` vira `role="alert"` e é anunciada de imediato; ligue o `id` ao `aria-describedby` do campo para associar mensagem e controle.

Disponível em `@portais-orion/blocks`, na categoria **Formulários**.

## Instalação

```bash
# O ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<ComponentPreview name="blocks/form-message" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `tone` | `"default" \| "error" \| "warning" \| "success"` | `default` |
| `className` | `string` | — |
| `id` | `string` | — |

Além destas, `FormMessage` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Error story

<ComponentPreview name="blocks/form-message" story="ErrorStory" />

### Warning

<ComponentPreview name="blocks/form-message" story="Warning" />

### Success

<ComponentPreview name="blocks/form-message" story="Success" />

## Storybook

Todas as variações de `FormMessage`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

