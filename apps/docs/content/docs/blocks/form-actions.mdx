---
title: FormActions
description: Componente da biblioteca Blocks.
---

import { FormActions } from "@portais-orion/blocks";

# FormActions

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Forms**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { FormActions } from "@portais-orion/blocks";

export default function Example() {
  return (
    <FormActions {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### LeftAligned
Consulte o Storybook para a variação `LeftAligned`.

### Between
Consulte o Storybook para a variação `Between`.

### Sticky
Consulte o Storybook para a variação `Sticky`.

### WithExtra
Consulte o Storybook para a variação `WithExtra`.
