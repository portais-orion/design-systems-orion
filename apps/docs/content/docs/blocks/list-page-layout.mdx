---
title: ListPageLayout
description: Componente da biblioteca Blocks.
---

import { ListPageLayout } from "@portais-orion/blocks";

# ListPageLayout

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Layout**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { ListPageLayout } from "@portais-orion/blocks";

export default function Example() {
  return (
    <ListPageLayout {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### WithStats
Consulte o Storybook para a variação `WithStats`.

### WithToolbar
Consulte o Storybook para a variação `WithToolbar`.

### WithFilters
Consulte o Storybook para a variação `WithFilters`.

### WithDataTable
Consulte o Storybook para a variação `WithDataTable`.

### LoadingTable
Consulte o Storybook para a variação `LoadingTable`.

### EmptyTable
Consulte o Storybook para a variação `EmptyTable`.
