---
title: FilterPill
description: Componente da biblioteca Blocks.
---

import { FilterPill } from "@portais-orion/blocks";

# FilterPill

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Tabelas**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { FilterPill } from "@portais-orion/blocks";

export default function Example() {
  return (
    <FilterPill {/* props */} />
  )
}
```

## Exemplos

### Default
```tsx
<FilterPill {...{ label: "Status", value: "Ativo", onRemove: () => {}} />
```

### SemValor
```tsx
<FilterPill {...{ label: "Somente pendentes", onRemove: () => {}} />
```

### SemRemocao
```tsx
<FilterPill {...{ label: "Período", value: "Últimos 30 dias" }} />
```

### Grupo
Consulte o Storybook para a variação `Grupo`.
