---
title: TableToggle
description: "Switch compacto (36×20) para alternar um valor booleano diretamente numa célula de tabela, sem label ao lado."
---

Para alternância em formulário, use o `Switch` padrão do `@design-systems-orion/ui`.

Disponível em `@design-systems-orion/blocks`, na categoria **Tabelas**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @design-systems-orion/ui junto.
pnpm add @design-systems-orion/blocks
```

## Import

```tsx
import { TableToggle } from "@design-systems-orion/blocks/table-toggle";
```

O import por subpath traz só `table-toggle`. Importar de `@design-systems-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/table-toggle" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `checked`\* | `boolean` | — |
| `onToggle`\* | `() => void` | — |
| `disabled` | `boolean` | `false` |
| `aria-label` | `string` | — |
| `className` | `string` | — |

\* Prop obrigatória.

Além destas, `TableToggle` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Disabled

<ComponentPreview name="blocks/table-toggle" story="Disabled" />

## Storybook

Todas as variações de `TableToggle`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

