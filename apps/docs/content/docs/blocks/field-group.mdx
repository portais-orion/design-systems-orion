---
title: FieldGroup
description: Componente da biblioteca Blocks.
---

import { FieldGroup } from "@portais-orion/blocks";

# FieldGroup

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Forms**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { FieldGroup } from "@portais-orion/blocks";

export default function Example() {
  return (
    <FieldGroup {/* props */} />
  )
}
```

## Exemplos

### OneColumn
Consulte o Storybook para a variação `OneColumn`.

### TwoColumns
Consulte o Storybook para a variação `TwoColumns`.

### ThreeColumns
Consulte o Storybook para a variação `ThreeColumns`.

### FourColumns
Consulte o Storybook para a variação `FourColumns`.

### WithMixedFields
Consulte o Storybook para a variação `WithMixedFields`.
