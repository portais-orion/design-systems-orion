---
title: ContentCard
description: Componente da biblioteca Blocks.
---

import { ContentCard } from "@portais-orion/blocks";

# ContentCard

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Data Display**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { ContentCard } from "@portais-orion/blocks";

export default function Example() {
  return (
    <ContentCard {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### WithHeader
Consulte o Storybook para a variação `WithHeader`.

### WithActions
Consulte o Storybook para a variação `WithActions`.

### NestedSections
Consulte o Storybook para a variação `NestedSections`.
