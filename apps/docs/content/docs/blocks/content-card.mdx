---
title: ContentCard
description: "Painel de conteúdo com cabeçalho opcional (título, descrição e ações)."
---

Monta a estrutura de `Card` mais comum sem que você repita header e content a cada uso; o cabeçalho só é renderizado se houver algum deles.

Disponível em `@portais-orion/blocks`, na categoria **Exibição de dados**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @portais-orion/ui junto.
pnpm add @portais-orion/blocks
```

## Import

```tsx
import { ContentCard } from "@portais-orion/blocks/content-card";
```

O import por subpath traz só `content-card`. Importar de `@portais-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/content-card" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `title` | `any` | — |
| `description` | `any` | — |
| `actions` | `any` | — |
| `className` | `string` | — |
| `contentClassName` | `string` | — |

Além destas, `ContentCard` aceita as props do elemento/primitivo que envolve.

## Exemplos

### With header

<ComponentPreview name="blocks/content-card" story="WithHeader" />

### With actions

<ComponentPreview name="blocks/content-card" story="WithActions" />

### Nested sections

<ComponentPreview name="blocks/content-card" story="NestedSections" />

## Storybook

Todas as variações de `ContentCard`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

