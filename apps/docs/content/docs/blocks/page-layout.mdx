---
title: PageLayout
description: "Esqueleto de uma página: padding, largura máxima e o espaçamento vertical padrão entre header, conteúdo, coluna lateral e footer."
---

Não inclui sidebar nem breadcrumb — isso é o `AppShell`.

Disponível em `@design-systems-orion/blocks`, na categoria **Layout**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @design-systems-orion/ui junto.
pnpm add @design-systems-orion/blocks
```

## Import

```tsx
import { PageLayout } from "@design-systems-orion/blocks/page-layout";
```

O import por subpath traz só `page-layout`. Importar de `@design-systems-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/page-layout" story="Default" />

## API Reference

| Prop | Tipo | Padrão | Descrição |
| --- | --- | --- | --- |
| `variant` | `"plain" \| "list" \| "form" \| "detail" \| "dashboard"` | `plain` | Escolhe a largura e o espaçamento da coluna de conteúdo. |
| `header` | `any` | — | — |
| `aside` | `any` | — | Coluna lateral de 20rem à direita do conteúdo. |
| `footer` | `any` | — | — |
| `className` | `string` | — | — |
| `contentClassName` | `string` | — | — |
| `maxWidth` | `"none" \| "screen-xl" \| "screen-2xl"` | `none` | — |

Além destas, `PageLayout` aceita as props do elemento/primitivo que envolve.

## Exemplos

### With footer

<ComponentPreview name="blocks/page-layout" story="WithFooter" />

### Narrow content

<ComponentPreview name="blocks/page-layout" story="NarrowContent" />

### Full width

<ComponentPreview name="blocks/page-layout" story="FullWidth" />

## Storybook

Todas as variações de `PageLayout`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

