---
title: DataTable
description: Componente da biblioteca Blocks.
---

import { DataTable } from "@portais-orion/blocks";

# DataTable

Um componente da biblioteca `@portais-orion/blocks` agrupado em **Tabelas**.

## Instalação

```bash
# Como nosso ecossistema é um monorepo e importamos o package completo:
pnpm add @portais-orion/blocks
```

## Uso

<Preview>
  <div className="text-muted-foreground text-sm">
    Substitua este texto pela declaração real do componente com suas props obrigatórias.
  </div>
</Preview>

```tsx
import { DataTable } from "@portais-orion/blocks";

export default function Example() {
  return (
    <DataTable {/* props */} />
  )
}
```

## Exemplos

### Default
Consulte o Storybook para a variação `Default`.

### Loading
Consulte o Storybook para a variação `Loading`.

### Empty
Consulte o Storybook para a variação `Empty`.

### ErrorStory
Consulte o Storybook para a variação `ErrorStory`.

### WithPagination
Consulte o Storybook para a variação `WithPagination`.

### WithActions
Consulte o Storybook para a variação `WithActions`.

### WithRowClick
Consulte o Storybook para a variação `WithRowClick`.

### WithToolbar
Consulte o Storybook para a variação `WithToolbar`.

### WithSorting
Consulte o Storybook para a variação `WithSorting`.

### DenseOperationalExample
Consulte o Storybook para a variação `DenseOperationalExample`.

### SupertransInspiredExample
Consulte o Storybook para a variação `SupertransInspiredExample`.

### AuroraInspiredExample
Consulte o Storybook para a variação `AuroraInspiredExample`.
