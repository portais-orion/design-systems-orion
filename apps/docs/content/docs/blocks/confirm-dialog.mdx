---
title: ConfirmDialog
description: "Modal de confirmação para uma ação, com estado de carregamento embutido."
---

Só fecha quando `onConfirm` resolve: se a promessa rejeitar, o modal permanece aberto e cabe ao chamador exibir o erro. Use `variant="danger"` para ações destrutivas.

Disponível em `@portais-orion/blocks`, na categoria **Feedback**.

## Instalação

```bash
# Primitives e blocks são packages separados: instale só o que for usar.
# Os blocks compõem as primitives, então este já traz @portais-orion/ui junto.
pnpm add @portais-orion/blocks
```

## Import

```tsx
import { ConfirmDialog } from "@portais-orion/blocks/confirm-dialog";
```

O import por subpath traz só `confirm-dialog`. Importar de `@portais-orion/blocks` também
funciona e dá no mesmo bundle — os packages são `sideEffects: false`.

## Uso

<ComponentPreview name="blocks/confirm-dialog" story="Default" />

## API Reference

| Prop | Tipo | Padrão |
| --- | --- | --- |
| `open`\* | `boolean` | — |
| `onOpenChange`\* | `(open: boolean) => void` | — |
| `title`\* | `string` | — |
| `description` | `string` | — |
| `confirmLabel` | `string` | `Confirmar` |
| `cancelLabel` | `string` | `Cancelar` |
| `variant` | `"default" \| "danger"` | `default` |
| `isLoading` | `boolean` | `false` |
| `onConfirm`\* | `() => void \| Promise<void>` | — |

\* Prop obrigatória.

Além destas, `ConfirmDialog` aceita as props do elemento/primitivo que envolve.

## Exemplos

### Danger

<ComponentPreview name="blocks/confirm-dialog" story="Danger" />

### Com falha

<ComponentPreview name="blocks/confirm-dialog" story="ComFalha" />

## Storybook

Todas as variações de `ConfirmDialog`, com controles interativos, vivem no
[Storybook](http://localhost:6006) do monorepo.

