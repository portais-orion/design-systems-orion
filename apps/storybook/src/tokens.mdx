import { Meta, Title } from "@storybook/addon-docs/blocks";

<Meta title="FundaÃ§Ãµes/Tokens" />

<Title>Tokens e temas</Title>

## Modelo

Duas camadas em `@supertrans-transportes/tokens`:

1. **`base.css`** â€” o mapeamento semÃ¢ntico (`@theme inline` do Tailwind v4) + defaults neutros. NÃ£o contÃ©m nenhuma cor de marca.
2. **`themes/<marca>.css`** â€” somente valores de identidade da marca (primary, hover, accent, sidebar), aplicados via CSS variables.

## Seletor de tema

Cada tema define exatamente trÃªs seletores (nunca `:root, [data-brand]` simples â€” empata especificidade e o Ãºltimo import venceria sempre):

```css
:root:not([data-brand]),          /* fallback sem marca definida        */
:root[data-brand="aurora"],       /* marca ativa no elemento raiz       */
[data-brand="aurora"] { ... }     /* containers aninhados (previews)    */
```

## Marcas disponÃ­veis

| Marca | primary | hover | accent | sidebar |
|---|---|---|---|---|
| **supertrans** | azul petrÃ³leo | mais claro | ciano | navy |
| **aurora** | laranja | mais escuro | laranja claro | slate |

Use a toolbar **Marca** deste Storybook, ou a story *NÃºcleo â†’ Comparativo de Marcas* para ver as duas lado a lado.

## Uso nos produtos

```css
@import "@supertrans-transportes/tokens/base.css";
@import "@supertrans-transportes/tokens/themes/aurora.css";  /* UM tema por produto */
```

Ambientes multi-marca (como este Storybook) importam `@supertrans-transportes/tokens/index.css` e controlam a marca por `data-brand` no elemento raiz.

## Regras

- **Proibido** hex em componentes â€” hex existe apenas dentro de `packages/tokens/src/themes/`.
- **Proibido** classes de marca (`orange-*`, `blue-*`, `primary-600`) â€” use `bg-primary`, `bg-primary-hover`, `text-muted-foreground`, `border-border`.
- VariaÃ§Ãµes de cor sÃ£o tokens (`--primary-hover`), nunca escala numÃ©rica.
- Token novo: mapeie no `@theme` do base.css, dÃª default neutro em `:root` e defina o valor em **todos** os temas (tema incompleto quebra a outra marca silenciosamente).
- Marca nova = um arquivo `themes/<marca>.css` (workflow `ai/workflows/add-brand-theme.md`).
