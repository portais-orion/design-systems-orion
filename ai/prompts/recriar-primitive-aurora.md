# Prompt: recriar primitive do Aurora

> Recrie o componente `<NOME>` (existente no Portal-Aurora em `src/components/ui/<Nome>.tsx`) em `packages/ui/src/<nome>/` seguindo `ai/workflows/recreate-from-aurora.md`. Use a implementação do Aurora somente como referência de API; implemente sobre `@base-ui/react/<nome>` com Tailwind v4 e tokens semânticos (nada de Radix, nada de `bg-white`/`text-gray-*`/`primary-500`). Story + validação nas duas marcas + checklist + changeset.
